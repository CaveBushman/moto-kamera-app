# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Runtime
logs/
captures/
*.log

# Hailo models: large compiled binaries, downloaded per-machine by
# scripts/setup_hailo.sh -- kept out of git (see models/README.md).
*.hef
models/*.hef

# OS
.DS_Store

# Local config with real device addresses/IPs may be sensitive -- commit
# config/config.yaml as the shared template, ignore machine-local copies.
config/config.local.yaml
